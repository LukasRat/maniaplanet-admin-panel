{
  "scripts": {
    "prestart": "node -e \"if (!require('fs').existsSync('node_modules')) { console.error('\\n❌ Error: Dependencies not installed!\\n\\nPlease run: npm install\\n'); process.exit(1); }\"",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "gbxremote": "^0.2.1",
    "multer": "^2.0.2"
  }
}